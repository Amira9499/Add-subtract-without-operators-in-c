#include <stdio.h>
#include <stdlib.h>
#define N 5



int main()
{ int a[N], *p;
printf("Enter %d elements in the array: \n",N);
for(p=a;p<=a+N-1;p++)
{
    scanf("%d",p);
}

printf("elements in reverse order : \n");
for(p=a+N-1;p>=a;p--)
{
    printf("%d \n",*p);
}

return 0;
}
