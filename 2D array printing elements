#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    int a[2][2]={1,2,3,4};
    int *p = &a[0][0];
    for (p=&a[0][0];p<=&a[1][1];p++)
    {
        printf("%d \n",*p);
    }
    return 0;
}
